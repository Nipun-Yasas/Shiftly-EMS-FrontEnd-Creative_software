
export const metadata = {
  title: "Refer Candidate",
  description: "Refer Candidate",
};

export default function ReferLayout({ children }) {
  return (
    <html lang="en">
      <body >
        {children}
      </body>
    </html>
  );
}
