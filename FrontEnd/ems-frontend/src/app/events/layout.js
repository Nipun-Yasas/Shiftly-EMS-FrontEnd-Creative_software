
export const metadata = {
  title: "Events",
  description: "Events",
};

export default function EventsLayout({ children }) {
  return (
    <html lang="en">
      <body >
        {children}
      </body>
    </html>
  );
}
