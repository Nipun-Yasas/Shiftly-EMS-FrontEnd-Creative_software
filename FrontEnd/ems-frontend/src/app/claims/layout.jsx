
export const metadata = {
  title: "Claim",
  description: "Claim",
};

export default function ClaimLayout({ children }) {
  return (
    <html lang="en">
      <body >
        {children}
      </body>
    </html>
  );
}
